// ltl-whois/main.ltl
//
// RDAP and legacy-WHOIS lookup tool for domains and IPs.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-whois v0.1.0")
    io::println("RDAP and legacy-WHOIS lookup tool for domains and IPs.")
    0
}
